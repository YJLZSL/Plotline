# 交接文档 — Plotline v4.0.0（深度创作首批，已提交发布）

> 本文档记录当前版本状态、已完成工作、遗留问题与下一步任务，供下一任 AI 快速接手。

## 当前版本

- **代码版本**：v4.0.0（相对时间轴纯故事顺序标尺 + 地图图层 + VN 立绘拖拽 + 世界观冲突检测 + 日文翻译；已打 `v4.0.0` tag）
- **最新已发布**：v4.0.0（CI Release 由 workflow 自动生成）
- **上一版本**：v3.6.0（时间轴界面重做）
- **本地构建状态**：lint / typecheck / check:version 全绿，685 个单测全绿，全量 E2E 88/88 通过；Rust 无改动，依赖 CI 验证
- **自动更新**：发布链路未变（`.sig` / `latest.json` 由 CI 自动生成）

## 本轮改动（v4.0.0：深度创作首批）

### 1. 时间轴相对标尺彻底重做
- 相对模式**完全移除伪日历刻度**，改为 `#1 → #2 → #3` 大节点连线；
- 每个节点下方显示事件自己的相对日期（如"第1天"）；缩放档位显示"相对"；
- 相对模式不渲染日历网格与 Today 线；保留 `computeEventTimePositions` 单一坐标源。

### 2. 地图分组 / 图层
- 新增 `mapLayers` 持久化 store；地图视图新增图层面板（新建/重命名/改色/删除/显隐）；
- 地点可分配到唯一分组；关闭分组可见性时对应地点在地图上隐藏。

### 3. VN 立绘插槽拖拽
- 预览播放器中立绘可自由拖拽定位，按画布百分比持久化，双击恢复预设位置。

### 4. 世界观冲突检测
- 世界观视图检测跨分类重复/近重复条目标题并展示冲突面板。

### 5. AI Agent 工作流切片
- `aiContext.pendingAction`：时间轴 More 菜单"AI 检查时间轴漏洞"打开面板后自动执行。

### 6. 日文翻译与无障碍
- 新增 `ja.json`（845 个 key 与 zh/en 完全一致），设置页支持日本語。
- 事件卡片、轨道 lane 增加 aria-label。

## 上一轮改动（v3.6.0：时间轴界面重做）

### 1. 相对时间轴不再空旷
- 新增 `compactBounds`：相对模式标尺/网格/画布宽度按事件实际位置 + 步长余量计算，不再为 2 个事件预留 8–10 年。
- 标尺新增 `#1` / `#2` 相对事件顺序标记（`relativeMarkers`）；缩放档位显示"相对"；隐藏"今天"参考线。

### 2. 标尺 / 卡片 / 轨道视觉重做
- 标尺：主刻度间交替时间带、主刻度加粗、accent 底部刻度，高度 44→52。
- 事件卡片：10px 圆角、柔和双层阴影、1px 边框；时间范围放入带日历图标的浅色胶囊；轨道色条加宽；标题与留白层级收紧。
- 轨道：斑马纹更清晰、hover 高亮、拖放目标 ring。

### 3. v4.0 AI Agent 工作流切片
- `aiContext` 新增 `pendingAction`；时间轴 More 菜单新增"AI 检查时间轴漏洞"，打开 AI 面板后自动执行 `check_timeline_consistency`。

### 4. 竞品调研
- 新增 `docs/v3.6.0-research.md`：Aeon Timeline（Subway View）、Plottr Timeline、Eleken 通用时间轴 UX 原则。

### 5. 测试与截图
- 新增 `timeline-relative-mode.spec.ts`（紧凑范围 / `#1#2` 标记 / 无 Today）、`aiContext` pendingAction 测试、`AiAssistantPanel` 自动执行测试。
- 截图留证：`docs/截图留证/v3.6.0-timeline-redesign.png`。

## 上一轮改动（v3.5.0：番茄钟 × 写作闭环）

### 1. C1 写作目标绑定
- 新增 `src/stores/writingGoals.ts`：每日/每周目标（默认 1000/7000 字）、逐日字数与专注番茄记录、趋势纯函数。
- 设置页「数据」tab 新增写作目标输入；小说编辑器在番茄钟专注中新增字数自动计入当天目标（章节切换不计入）。
- 番茄钟完成专注自动写入当天 1 个番茄；面板展示当日字数进度条与达成状态。

### 2. C2 专注统计
- 统计视图新增：今日字数/今日专注/本周字数/本周专注 4 张卡片 + 近 7 天专注柱状图 + 近 8 周写作柱状图。
- 无事件但有专注记录时统计页不再显示空白状态。

### 3. C3 系统通知
- Rust：新增 `tauri-plugin-notification` 依赖、`commands/pomodoro.rs`（`send_pomodoro_notification`）、capability `notification:default`。
- 前端：`src/features/pomodoro/api.ts` 封装命令；专注/休息结束发送系统通知，web 模式或命令失败回退浏览器 Notification API。

### 4. C4 里程碑 + B4 MC 强调色
- 每完成 4 个番茄消费 `pomodoro.milestone` 提示里程碑。
- 设置页新增自由取色器与「MC 主题也应用自定义强调色」开关（persist 在 `plotline:ui`），开启后以内联 `--accent` 覆盖 MC 草绿。

### 5. E2E 稳定性
- `settings-view.spec.ts` beforeEach/分组合切换增加显式等待与点击重试，修复全量并行运行时的启动/挂载竞态。

## 上一轮改动（v3.4.0：整体重构，已并入本版本发布）

v3.4.0 按用户反馈对"时间轴奇怪 / 信息不清晰 / 时间对齐有问题 / AI 功能与模块协作欠缺 / 视觉效果与开场动画"做了整体重构，全部为前端 + 脚本层改动，**无数据库结构变化**：

### 1. 时间轴对齐与清晰度
- **共享时间位置源**：新增 `computeEventTimePositions`（`timelineGrid.ts`），`computeTimelineLayout` 与 `TimelineView` 连线层消费同一份位置；相对事件按相对事件序列下标排布，修复绝对/相对混合时"相对事件凭空跳位"与连线锚点错位。
- **真实日历网格背景（A9）**：新增 `timelineGridBackground.ts`，网格线由真实刻度边界经 `getXAtTime` 生成，与 DateRuler 像素级对齐。
- **标尺自适应与降级（A6）**：`chooseAdaptiveTickLevel` / `sampleTickTimestamps` 控制渲染预算；`formatMajorTickDegraded` 放不下时降级为年份而不是跳过大段。
- **卡片宽度复合估算（A2）**：`getEventCardWidthForEvent` 综合标题/时间/地点/角色，可读下限 232px。
- **子行视觉分隔（A3）**：多子行轨道渲染虚线分隔 + 奇数子行背景交替。
- **折叠轨道连线（A4）**：折叠轨道入/出边跳过，锚点收拢到折叠条中心。
- **连线手柄发现性（A8）**：首次进入时间轴 4.5s 内手柄常亮；**Script 按钮激活态（A10）**。
- **吸附一致性**：吸附改为最近**可见主刻度**（`getSnapTimeAtX` 可选 tickTimes + `getSnapThresholdForTicks`），吸附线/标尺/网格三线合一；纯相对时间轴锚定当前 UTC 年首日；纯绝对时间轴不再被无谓拉长 8 年。
- **相对时间轴提示**：新增 `timeline.relativeOnlyHint` 标尺角标。

### 2. AI 功能与模块协作
- **会话与缓存统一**：`ai-assistant` 复用 `aiSessionsKey`/`aiMessagesKey`，浮动 AI 面板与"AI 创作"独立视图共享 `currentSessionId`，不再出现两份会话状态。
- **选中上下文打通**：`buildAssistantContext` 自动读取 `useAiContextStore` 的全局选中事件/角色；AI 创作视图展示当前选中对象。
- **跨工作区会话回退**：切换工作区时若共享会话不属于当前工作区，自动回退到第一个会话。
- **错误兜底**：流式请求在产生事件前失败时显示 `aiAssistant.sendFailed` 提示；连接测试 loading 从旋转图标改为脉冲圆点。

### 3. 开场动画与视觉
- **Splash 重构**：点击任意位置 / Enter / Space / Esc / 跳过按钮均可关闭；修复 web 模式点击中央内容无法跳过（原先 inner content `stopPropagation`）；文案 i18n；尊重减少动效与增强动效开关；进度条与羽毛笔编排更完整。
- **字体主题彻底化（B2/B3）**：RichEditor 增加"编辑器跟随界面字体主题"偏好（persist 到 localStorage）；FontSelector 拆分 `variant='ui' | 'editor'`，设置页外观/编辑器 tab 不再重复挂载。
- **Toolbar AI 按钮**：激活态加 ring，aria-pressed。

### 4. 工程
- **E2 check:version**：新增 `scripts/check-version.mjs` + `pnpm check:version`，挂入 `ci.yml` 前端 job。
- 版本号统一升级 `3.4.0`（package.json / Cargo.toml / tauri.conf.json / Cargo.lock）。

## 最近改动（v3.3.0：清晰度与一致性）

v3.3.0 是「长期迭代计划」Phase 1 的首个交付，核心是**时间轴可读性、字体修复、i18n 补全与 CI 门禁**：

- **时间轴连接线可读性**：非激活连线 opacity 0.25→0.45、线宽 1→1.5、drop-shadow 光晕与轨道网格分离；目标端点圆点更醒目（`TimelineView.tsx` ConnectionPath）。
- **相对事件「相对」角标**：非绝对日期事件卡片显示 accent 角标（`EventCard.tsx`）。
- **draft 状态点实色**：`bg-text-secondary/60` → `bg-status-draft`（`EventCard.tsx`）。
- **Today 参考线贯穿轨道区**：与标尺内标记同坐标源（`TimelineView.tsx`）。
- **font-pixel / font-smiley 工具类首次可编译**：`tailwind.css` 补 `@theme inline` 映射（此前为死代码）。
- **en.json 补全 51 个 key**：zh/en 完全对齐（801/801），新增 `localeParity.test.ts`。
- **CI/Rust 门禁**：`ci.yml` 双 OS 矩阵 + clippy 门禁 + E2E timeline 子集 job；`release.yml` 签名校验前置 + notes 动态提取。
- **E2E 稳定性**：拖动吸附断言改 `expect.poll`，消除时序 flaky。

### 数据安全（本版本重点核对项）

- **无任何数据库结构改动**（无新迁移、无 Rust 模型变更）——用户工作区数据与 v3.2.1 完全兼容。
- **时间轴坐标/布局引擎零改动**（`timelineGrid` / `timelineLayout` / `timeScale`）——仅视觉层变更，事件位置、日期、轨道、连接数据不受影响。
- **自动更新签名链路未变**：`tauri.conf.json` pubkey 不变，`.sig` / `latest.json` 仍由 CI 自动生成。

v3.2.0 时间轴拖动吸附功能完整保留，关键文件如下：

- `src/features/timeline/timelineGrid.ts`：新增 `getSnapTimeAtX` / `getSnapXAtTime` 纯函数，作为吸附计算的单一源。
- `src/lib/motionOrchestrator.ts`：新增 `dragSnap` 场景预设，统一吸附确认、卡片归位与连接线重绘动画。
- `src/components/views/TimelineView.tsx` / `src/features/timeline/EventCard.tsx`：集成吸附目标计算与 `dragSnap` 归位动画。
- `src/components/ui/DragSnapHint.tsx` / `DragSnapTooltip.tsx`：拖拽时实时显示吸附时间与偏移量的提示组件。
- `tests/e2e/real-creative-intent.spec.ts`（或同目录对应文件）：真实创作流程 E2E，验证创建事件 → 拖动吸附 → 时间/位置一致。
- 版本号与文档：`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.lock`、`AGENTS.md`、`更新日志.md`、`docs/产品路线图.md`、本文件同步更新。

## 已完成工作（v3.1.0 本轮迭代）

### 1. 时间轴坐标单一源重构
- **问题**：v3.0.0 前标尺、事件卡片、连接线、Today 线各自实现 `timeToX`，多源坐标计算在缩放/平移/滚动后出现错位。
- **实现**：
  - 在 `src/features/timeline/timelineGrid.ts` 新增 `ViewportState` 类型与 `getXAtTime` / `getTimeAtX` 纯函数，作为时间↔像素换算的单一源。
  - 标尺、事件卡片定位、连接线锚点、Today 线全部改用 `viewportState` + `getXAtTime` / `getTimeAtX` 计算。
  - 修复 `setTimeRange` 从 hook 解构但从未调用导致 `viewportState.timeRange` 始终为默认值的 bug。
- **状态**：✅ 已完成，新增 `timelineGrid.test.ts` 中 `getXAtTime` / `getTimeAtX` 在年/月/日/时四档 zoom 下的精度测试（误差 ≤ 1px）

### 2. 时间轴标尺 6 档刻度自适应
- **问题**：原标尺刻度档位有限，在极端 zoom 下刻度密度过高或过低，甚至崩溃。
- **实现**：
  - `chooseTickLevel(zoom)` 在 `year` / `quarter` / `month` / `week` / `day` / `hour` 之间切换，主刻度显示完整年月日。
  - 对 `0` / `NaN` / `Infinity` 极端 zoom 做兜底；修复 `chooseTickLevel` 对 `Infinity` 返回 `'year'` 而非 `'hour'` 的问题。
- **状态**：✅ 已完成

### 3. 事件卡片三 zone 重构
- **问题**：原 `EventCard` 内部布局耦合较重，时间/地点/角色信息排版拥挤。
- **实现**：
  - 拆分为 header（状态点 + 标题 + 连接句柄）/ body（时间范围 + 持续时间）/ footer（地点 + 角色头像堆叠）三 zone。
  - 时间文本统一 `tabular-nums` 与 `whitespace-normal`。
  - `Event` 类型新增 `endDateTime` / `relativeTo` / `relativeOffsetDays` 可选字段。
  - 新增 `formatEventTimeRange` 输出 "2027-12-03 14:00 – 16:00" / "相对 #3 · +2d" / "2027" 等场景化格式。
- **状态**：✅ 已完成，新增 `EventCard.test.tsx` 三 zone 结构与角色头像堆叠测试、`time.test.ts` 时间格式化测试

### 4. 连接线时间锚点边缘对齐
- **问题**：原连接线起点/终点使用事件卡片视觉中心，拖动事件后连接线偏离锚点。
- **实现**：
  - 起点/终点改为事件卡片"时间锚点边缘"：同轨道左右沿中点 / 跨轨道上下沿中点。
  - 拖动事件后连接线始终贴合锚点。
- **状态**：✅ 已完成，新增 `timeline-connection-alignment.spec.ts` 3 个 E2E 测试（同轨道/跨轨道/滚动后对齐）

### 5. 动画编排层 motionOrchestrator
- **问题**：跨视图切换、批量元素入场、拖拽归位等动画分散在各组件，stagger / delay / exit timing 不统一。
- **实现**：
  - 新增 `src/lib/motionOrchestrator.ts`，提供 5 个场景预设：`viewSwitch` / `cardBatchEnter` / `dragReturnWithConnections` / `aiPanelExpand` / `sidebarNavEnter`。
  - 支持分层缓动与 `prefers-reduced-motion` 退化；`enhanced` 关闭时统一退化为 200ms 同步淡入。
- **状态**：✅ 已完成，新增 `motionOrchestrator.test.ts`（25 个测试覆盖预设生成、enhanced 门控、stagger 计算）

### 6. 工具栏四分组 + Sidebar 三分组
- **问题**：时间轴工具栏控件散乱，Sidebar 入口重复（AI 创作 / AI Studio 并存）。
- **实现**：
  - 时间轴工具栏拆分为 Create / Filter / View mode（segmented control）/ More（下拉菜单）四分组。
  - Sidebar 重组为工作区视图 / 创作辅助 / 系统三分组，新增 12 个 `nav.tooltip.*` 文案。
  - 首次进入工作区高亮"时间轴"与"AI 创作"入口（持续 3 秒 `ring-2 ring-accent/40`）。
  - 删除 Sidebar 中重复的"AI Studio"入口，仅保留"AI 创作"。
  - 工具栏新增"导出时间轴为 Markdown"功能。
- **状态**：✅ 已完成，新增 `timeline-toolbar.spec.ts` 9 个 E2E 测试

### 7. Visual E2E 测试套件
- **实现**：新增 14 个 visual E2E 测试（timeline-alignment / timeline-connection-alignment / animation-coherence / theme-consistency），含 dev server 不可用时自动 skip 机制。
- **状态**：✅ 已完成

### 8. 版本号与文档同步
- **实现**：`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.lock` 升级到 `3.1.0`；`AGENTS.md`、`更新日志.md`、本文件、`docs/产品路线图.md`、`docs/前端优化指南.md` 更新至 v3.1.0 状态。
- **状态**：✅ 已完成

## 上一版本迭代回顾（v3.0.0）

### 1. 时间轴逻辑重写
- **问题**：v2.8.0/v2.8.1 后时间轴拖拽锚点、事件重叠判定与相对/绝对事件混合布局仍存在体验问题。
- **实现**：
  - 新增 `src/features/timeline/timelineGrid.ts`、`timeScale.ts`、`useTimelineViewport.ts`，统一 zoom/scroll 视口管理。
  - 重写 `src/features/timeline/timelineLayout.ts` 碰撞检测与 drag constraints。
  - 更新 `src/components/views/TimelineView.tsx` 集成新布局引擎，支持以指针为锚点的缩放、画布平移。
- **状态**：✅ 已完成，新增 Timeline 平移/缩放 E2E 测试与布局回归测试

### 2. Timeline / Script 双视图双向同步
- **问题**：用户需要在时间轴与剧本阅读视图之间快速切换并保持上下文。
- **实现**：
  - 新增 `src/stores/workspaceSelection.ts` 全局选择状态。
  - 新增 `src/components/views/ScriptView.tsx` 独立剧本视图。
  - Sidebar 与路由注册 `/workspaces/:id/script`。
- **状态**：✅ 已完成，新增 `timeline-script-sync.spec.ts` E2E 测试与 `scriptSort.test.ts` 单元测试

### 3. AI 创作助手侧边模块
- **问题**：原有 AI 面板功能较单一，需要更明确的 Agent 工作流与会话管理。
- **实现**：
  - 新增 `src/features/ai-assistant/` 模块，含 7 个 Agent、上下文选择器、会话列表。
  - Sidebar 新增 `AI 创作` / `AI Studio` 导航入口，路由注册 `/workspaces/:id/ai-assistant`。
  - 复用现有 AI IPC 与 `mock.ts` 模拟后端。
- **状态**：✅ 已完成，新增 `ai-assistant.spec.ts` E2E 测试

### 4. Rust clippy 清理
- **实现**：修复 `src-tauri/src/models/ai.rs` 与 `src-tauri/src/services/ai.rs` 中 6 处已有 clippy 告警，确保 `-D warnings` 通过。
- **状态**：✅ 已完成（代码修改通过旧测试二进制验证，本地因环境 bug 无法重新编译验证）

- GitHub Release v3.0.0：<https://github.com/YJLZSL/Plotline/releases/tag/v3.0.0>

## 上一版本迭代回顾（v2.8.1）

### 1. 时间轴事件堆叠回归修复
- **问题**：v2.8.0 中创建多个事件时，同一轨道内的事件被错误分配到瀑布式子行，导致事件整体下移、占用额外垂直空间。
- **实现**：
  - 调整 `src/features/timeline/timelineLayout.ts` 的碰撞检测与瀑布式子行逻辑，确保非真正重叠的事件保持在一行内紧凑排布。
  - 在 `src/components/views/TimelineView.tsx` 中验证渲染位置与布局计算一致。
- **状态**：✅ 已完成，新增/更新时间轴子行分配回归测试

### 2. AI 助手输出稳定化
- **问题**：AI 助手流式输出时面板高度跳动、整段重渲染导致闪烁，长回复滚动不跟手。
- **实现**：
  - 为 AI 输出面板设置最小高度，减少布局偏移。
  - 采用增量纯文本渲染，仅更新新增内容。
  - 接入平滑滚动，长回复自动跟随最新输出。
- **状态**：✅ 已完成，新增 AI 输出稳定性单元/E2E 测试

### 3. 整体 UI 清爽化
- **问题**：v2.8.0 后工具栏、侧边栏、卡片、设置页与空状态的间距和阴影风格不够统一，视觉噪音较多。
- **实现**：
  - 统一各主要视图的间距 token 与卡片阴影层级。
  - 调整设置页与空状态的留白和文案，提升长时创作舒适度。
- **状态**：✅ 已完成，视觉回归通过 Playwright 截图对比

### 4. 测试提速
- **问题**：E2E 测试中存在较多 `waitForTimeout` 硬等待，本地运行耗时较长。
- **实现**：
  - 用显式条件等待替换不必要的 `waitForTimeout`。
  - 本地 Playwright 配置启用 2 workers，充分利用多核资源。
- **状态**：✅ 已完成，全量测试在本地稳定全绿（435 单元测试 + 39 E2E 用例）

## 上一版本迭代回顾（v2.8.0）

v2.8.0 基于 v2.7.5 的反馈，对前端动效、时间轴逻辑、设置页与新手引导进行系统性升级：

- **时间轴事件重叠修复与拖拽升级**：`src/features/timeline/timelineLayout.ts` 新增碰撞检测，时间重叠事件自动拆分到瀑布式子行；拖拽时显示 ghost 阴影与 snap hint，释放后播放归位动画。
- **动效系统 spring/layout token 与增强动效开关**：`src/lib/motion.ts` 扩展 token，`src/stores/ui.ts` 与 `src/components/views/SettingsView.tsx` 提供“增强动效”开关。
- **TimelineView 性能优化**：`React.memo`、`useMemo`、可视区裁剪，长滚动更流畅。
- **设置页重构**：分组、搜索、即时预览、中文说明。
- **首次进入工作区新手引导与各视图空状态优化**。
- **新增 `docs/前端优化指南.md`**。

- GitHub Release v2.8.0：<https://github.com/YJLZSL/Plotline/releases/tag/v2.8.0>

## 更早版本迭代回顾（v2.7.5）

v2.7.5 修复了 v2.7.4 CI 中发现的 RAG 关键词检索 `LIKE` 子查询缺少 `%` 通配符问题（`src-tauri/src/services/ai.rs`），导致检索结果始终返回 0 条；补全通配符后确保 AI RAG 检索能正确返回相关实体，CI/Release workflow 全绿。

- GitHub Release v2.7.5：<https://github.com/YJLZSL/Plotline/releases/tag/v2.7.5>

## v2.8.1 迭代状态

### 目标

修复 v2.8.0 中时间轴多个事件被错误堆叠到下方的回归问题，并快速打磨 AI 助手输出稳定性与整体 UI 清爽度，发布 v2.8.1 patch 版本。

### 已完成工作

- 时间轴事件堆叠回归修复：调整碰撞检测与瀑布式子行逻辑，保证非真正重叠的事件在一行内紧凑排布。
- AI 助手输出稳定化：最小高度、增量纯文本渲染、平滑滚动。
- 整体 UI 清爽化：统一工具栏/侧边栏/卡片/设置页/空状态的间距与阴影。
- 测试提速：减少 `waitForTimeout` 硬等待，本地 Playwright 启用 2 workers。
- 版本号统一升级到 `2.8.1`（`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.lock`）。
- `AGENTS.md`、`更新日志.md`、本文件、`docs/产品路线图.md` 更新至 v2.8.1 状态。
- 验证套件：`pnpm lint`、`pnpm typecheck`、`pnpm test:run`（435 单元测试）、`pnpm test:e2e`（39 E2E 用例）全绿；`cargo test` 与 `cargo clippy` 预期在 CI 中全绿。

### 发布与签名状态

- Release 签名自 v2.5.3 起已完全自动化；v2.8.1 继续由 CI 自动生成 `.exe`、`.msi`、`.sig` 与 `latest.json` 并上传 Release。
- 无需使用本地 `keys/plotline.key` 手动签名；本地密钥仅作为 CI Secret 失效时的紧急 fallback。
- 推送 tag `v2.8.1` 后，Release Workflow 的 `Sign installer and upload updater manifest` 步骤预期自动生成 `.sig` 与 `latest.json`。

### 下一版本建议（v3.2+）

- 地图：地点分组/图层、打印/PDF 导出、角色足迹连线。
- VN：角色立绘插槽拖拽、完整预览播放器、导出 Ren'Py 增强。
- 世界观：种族/物种/宗教等实体管理、设定冲突检测。
- AI：Agent 式工作流，自动整理大纲/发现时间轴漏洞；AI 输出版本回溯。
- UI 美术：统一空状态插画、卡片质感升级、日文翻译补全。
- 番茄钟：与写作目标绑定、每日统计、系统通知提醒。

## 🔐 签名密钥状态（更新于 2026-06-28）

**密钥位置**：本地仓库 `keys/plotline.key`（私钥）、`keys/plotline.key.pub`（公钥）
- 公钥内容：`dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDE1NDIwNDk4N0NBRTAxQzcKUldUSEFhNThtQVJDRlF0dk1EOGRRdzhUQkxKOWdCREpqdUpYMEVmMXF2bG1acTFSU3lodjVBNGoK`
- 与 `src-tauri/tauri.conf.json` 中的 `pubkey` 字段 **完全匹配** ✅
- `keys/` 在 `.gitignore` 中，**不会提交到 GitHub**

**GitHub Secret 状态**：`TAURI_SIGNING_PRIVATE_KEY` 已配置（2026-06-27 更新）

**自动更新签名状态**：
- v2.5.3 起，`.sig` 与 `latest.json` 已由 CI 自动生成并上传 Release，不再需要手动签名。
- CI 签名步骤依赖 GitHub Secret `TAURI_SIGNING_PRIVATE_KEY`；该 Secret 已配置且验证通过。
- **紧急 fallback**：若 CI 失败或 Secret 失效，可临时使用本地 `keys/plotline.key` 手动签名：
  1. 下载 CI 构建的 `.exe` 文件
  2. 使用 `tauri signer sign --private-key-path keys/plotline.key -- Plotline_x.y.z_x64-setup.exe`
  3. 将生成的 `.sig` 文件上传到 Release assets
  4. 创建 `releases/vx.y.z/latest.json` 并填入正确的签名和 URL

## 遗留问题与下一步任务

> **长期目标**：见 `docs/长期迭代计划.md`。v3.3.0 已完成 A1/A5/A7 + B1 + D1 + E1；v3.4.0 已完成 A2/A3/A4/A6/A8/A9/A10 + B2/B3 + E2 + AI 协作 + Splash 重构；v3.5.0 已完成 C1–C4 + B4；v3.6.0 已完成时间轴界面重做；v4.0.0 已完成深度创作首批（地图图层、VN 立绘拖拽、世界观冲突检测、AI 工作流切片、日文翻译）。当前进入 **v4.1+「打磨发布」**。

### v4.0 重点

1. **地图增强**：地点分组/图层开关、角色足迹连线、打印/PDF/图片导出。
2. **VN 增强**：角色立绘插槽拖拽、完整预览播放器（分支选择）、Ren'Py 导出增强。
3. **世界观实体**：种族/物种/宗教/职业实体、与角色/地点/事件关联、设定冲突检测。
4. **AI Agent 工作流**：自动整理大纲、发现时间轴漏洞、长上下文 RAG、输出版本回溯。

### 中优先级

1. **日文翻译补全**：新建 `ja` locale 并对齐 zh/en key。
2. **无障碍/性能**：键盘导航全覆盖、1000+ 事件滚动优化、对比度 AA。
3. **CI 扩展**：timeline E2E 子集稳定后可逐步扩展到全量 `pnpm test:e2e`（visual 截图先解决跨平台基线）。

## 构建命令

```bash
# 版本一致性
node scripts/check-version.mjs   # 或 pnpm check:version

# 前端检查
./node_modules/.bin/eslint . --max-warnings=0
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run

# 时间轴 E2E 门禁
./node_modules/.bin/playwright test tests/e2e/timeline-

# Rust 检查（需 cargo 可用）
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
```

---

## 附录：项目整理记录（2026-06-27）

### 本次整理内容

1. **安全与隐私检查**：
   - ✅ `.gitignore` 完整：`keys/`、`.env`、`*.db`、`releases/**/*.exe`、`.msi`、`.sig` 已排除
   - ✅ 远程 GitHub 仓库无 `keys/` 目录、无 `.env` 文件、无数据库文件

2. **文档整理**：
   - 更新 `交接文档.md` 至 v2.7.4 状态
   - 更新 `更新日志.md` 添加 v2.7.4 条目
   - 更新 `AGENTS.md` 与 `docs/产品路线图.md` 至 v2.7.4 状态
   - 所有文档状态更新为 v2.7.4 有效

3. **生产/环境/测试隔离**：
   - ✅ 开发配置仅在 `tauri.conf.json` 中，无生产环境密钥泄露
   - ✅ 构建产物在 `.gitignore` 中，不提交
   - ✅ 无 `.env` 文件、无数据库文件、无日志文件在仓库中

### 密钥状态

| 项目 | 状态 |
|---|---|
| 私钥（`keys/plotline.key`） | ✅ 本地存在，已加入 `.gitignore`，未提交 |
| 公钥（`keys/plotline.key.pub`） | ✅ 本地存在，已加入 `.gitignore`，未提交 |
| 公钥（`tauri.conf.json`） | ✅ 已嵌入客户端配置 |
| GitHub Secret（`TAURI_SIGNING_PRIVATE_KEY`） | ✅ 已配置（2026-06-26 更新） |

### 快速上手指南

新 AI 或开发者接手时，按以下顺序操作：

1. **读文档**（5 分钟）：
   - `AGENTS.md` → `交接文档.md` → `docs/密钥管理指南.md` → `产品需求与设计文档.md`

2. **检查环境**（2 分钟）：
   - 确认 `keys/plotline.key` 存在（如缺失，联系项目 owner）
   - 确认 `node_modules/` 已安装（如缺失，运行 `pnpm install`）

3. **运行项目**（2 分钟）：
   - 前端：`./node_modules/.bin/vite` 或 `pnpm dev`
   - 完整（含 Rust）：`pnpm tauri dev`（需 Rust 环境）

4. **修改前检查**（3 分钟）：
   - 读 `AGENTS.md` §1 仓库导航表，定位要修改的文件
   - 读至少 1 个相邻同类文件，理解现有约定
   - 执行检查：`./node_modules/.bin/tsc --noEmit` + `./node_modules/.bin/vitest run`

---

## 历史版本发布记录（自 AGENTS.md 迁入）

> 本节保留各已发布版本的一句话摘要与 Release 链接；详细变更内容以 `更新日志.md` 为准。

- v3.1.0：时间轴对齐重写与动画编排层。时间轴坐标单一源（`ViewportState` + `getXAtTime`/`getTimeAtX`）、标尺 6 档自适应、事件卡片三 zone 重构、连接线时间锚点边缘对齐、动画编排层 5 场景预设、工具栏四分组 + Sidebar 三分组、14 个 visual E2E 测试。前端 `pnpm lint/typecheck/build/test:run` 全绿。
- GitHub Release v3.1.0：<https://github.com/YJLZSL/Plotline/releases/tag/v3.1.0>
- v3.0.0：跨视图叙事导航 + AI 创作工作流。时间轴布局引擎重写（`timelineGrid.ts` / `timeScale.ts` / `useTimelineViewport.ts`）、Timeline / Script 双视图双向同步（`workspaceSelection.ts` 全局选择状态 + `ScriptView.tsx`）、AI 创作助手侧边模块（7 个 Agent + 会话管理 + 上下文选择器）、Rust clippy 清理。前端 `pnpm lint/typecheck/build/test:run/test:e2e` 全绿。
- GitHub Release v3.0.0：<https://github.com/YJLZSL/Plotline/releases/tag/v3.0.0>
- v2.8.0：前端丝滑化与时间轴逻辑升级。时间轴事件重叠修复与拖拽升级、动效系统 spring/layout token 与增强动效开关、TimelineView 性能优化、设置页重构、首次进入工作区新手引导与各视图空状态优化、新增 `docs/前端优化指南.md`。
- GitHub Release v2.8.0：<https://github.com/YJLZSL/Plotline/releases/tag/v2.8.0>
- v2.7.5：修复 v2.7.4 CI 中发现的 RAG 关键词检索 `LIKE` 子查询缺少 `%` 通配符问题（`src-tauri/src/services/ai.rs`），导致检索结果始终返回 0 条；补全通配符后确保 AI RAG 检索能正确返回相关实体，CI/Release workflow 全绿。
- GitHub Release v2.7.5：<https://github.com/YJLZSL/Plotline/releases/tag/v2.7.5>
- v2.7.4：修复 v2.7.3 CI 中发现的 SQLite 错误 `ESCAPE expression must be a single character`（`src-tauri/src/services/ai.rs` RAG 检索中的 `LIKE ESCAPE` 子句）；v2.7.3 的所有功能改进完整保留，CI/Release workflow 全绿。
- GitHub Release v2.7.4：<https://github.com/YJLZSL/Plotline/releases/tag/v2.7.4>
- v2.7.3：修复 v2.7.2 CI 中发现的 Rust 编译错误（`src-tauri/src/services/ai.rs` 中 `NeedApi` 字段匹配不完整、`UpdateWorkspaceInput` 字段缺失、`entities` 借用/移动问题），v2.7.2 的所有功能改进完整保留。tag 已推送，但 CI/Release workflow 在 `cargo test` 阶段因 SQLite `ESCAPE expression must be a single character` 错误失败，未生成可用 Release。功能由 v2.7.4 完整发布。
- GitHub Release v2.7.3：<https://github.com/YJLZSL/Plotline/releases/tag/v2.7.3>（tag 已推送，Release 未成功；请使用 v2.7.4）
- v2.7.2：v2.7.0 体验问题深度修复（Markdown 渲染、动画性能、时间轴交互、AI 助手能力、番茄钟成就、MC 主题与字体兼容）。tag 已推送，但 CI/Release workflow 因 `src-tauri/src/services/ai.rs` 等处 Rust 编译错误失败，未生成可用 Release。功能由 v2.7.3 完整发布。
- GitHub Release v2.7.2：<https://github.com/YJLZSL/Plotline/releases/tag/v2.7.2>（tag 已推送，Release 未成功；请使用 v2.7.3）
- v2.7.0：全面打磨与 AI 增强（Markdown 渲染、动画性能、时间轴交互、AI 能力快捷栏、KV Cache/RAG、番茄钟成就重置、MC 主题趣味化）。
- GitHub Release v2.7.0：<https://github.com/YJLZSL/Plotline/releases/tag/v2.7.0>
- v2.6.2：修复 v2.6.1 Rust 测试编译错误，UI/UX 与 AI 助手改进完整保留。
- v2.6.1：时间轴布局修复、动画与设置页质感提升、AI 连接状态可见性（tag 已推送，但 Release 因 Rust 测试编译错误失败，功能由 v2.6.2 完整发布）。
- v2.6.0：AI 风格模板 chips、AI apply-to-doc、时间轴筛选折叠、模板向导空状态。
- v2.5.4：番茄钟可拖动、时间轴右键菜单、主题质感升级、AI 上下文增强。
- v2.3.0：时间轴连线修复、MC 主题配色重构、文本模式可见性、设置教程、番茄钟联动。
- v2.2.0：时间轴/事件逻辑重写、动画与性能优化、MC 主题再设计、工作区保存改进、自定义字体导入、真实浏览器测试。
- GitHub Release v2.6.0：<https://github.com/YJLZSL/Plotline/releases/tag/v2.6.0>
- GitHub Release v2.5.4：<https://github.com/YJLZSL/Plotline/releases/tag/v2.5.4>
- GitHub Release v2.3.0：<https://github.com/YJLZSL/Plotline/releases/tag/v2.3.0>
- GitHub Release v2.2.0：<https://github.com/YJLZSL/Plotline/releases/tag/v2.2.0>

---

> **文档版本**：v0.11.0
> **最后更新**：2026-08-15（v4.0.0 深度创作首批，已提交发布）
