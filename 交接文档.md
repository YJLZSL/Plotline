# 交接文档 — Plotline v3.0.0（未发布）

> 本文档记录当前版本状态、已完成工作、遗留问题与下一步任务，供下一任 AI 快速接手。

## 当前版本

- **版本号**：v3.0.0（未发布：跨视图叙事导航 + AI 创作工作流）
- **上一版本**：v2.8.1（时间轴回归修复与 UI 清爽化）
- **Git tag**：待推送 `v3.0.0`
- **GitHub Release**：待 CI 构建后生成
- **本地构建状态**：前端 `pnpm build` / `pnpm test:run` / `pnpm test:e2e` 全绿；本地 Rust 编译环境出现 Windows build script 子进程等待 bug，无法重新编译，但旧测试二进制 80 项单元测试仍通过
  - 预期产物：`Plotline_3.0.0_x64-setup.exe` + `Plotline_3.0.0_x64_en-US.msi` + `Plotline_3.0.0_x64-setup.exe.sig` + `latest.json`
- **自动更新**：待验证
  - 端点：`https://github.com/YJLZSL/Plotline/releases/latest/download/latest.json`
  - 自 v2.5.3 起，`.sig` 与 `latest.json` 由 CI 自动生成，无需手动签名

## 已完成工作（v3.0.0 本轮迭代）

### 1. 时间轴逻辑重写
- **问题**：v2.8.0/v2.8.1 后时间轴拖拽锚点、事件重叠判定与相对/绝对事件混合布局仍存在体验问题。
- **实现**：
  - 新增 `src/features/timeline/timelineGrid.ts`、`timeScale.ts`、`useTimelineViewport.ts`，统一 zoom/scroll 视口管理。
  - 重写 `src/features/timeline/timelineLayout.ts` 碰撞检测与 drag constraints。
  - 更新 `src/components/views/TimelineView.tsx` 集成新布局引擎，支持以指针为锚点的缩放、画布平移。
- **状态**：✅ 已完成，新增 Timeline 平移/缩放 E2E 测试与布局回归测试

### 2. Timeline / Script 双视图双向同步
- **问题**：用户需要在时间轴与剧本阅读视图之间快速切换并保持上下文。
- **实现**：
  - 新增 `src/stores/workspaceSelection.ts` 全局选择状态。
  - 新增 `src/components/views/ScriptView.tsx` 独立剧本视图。
  - Sidebar 与路由注册 `/workspaces/:id/script`。
- **状态**：✅ 已完成，新增 `timeline-script-sync.spec.ts` E2E 测试与 `scriptSort.test.ts` 单元测试

### 3. AI 创作助手侧边模块
- **问题**：原有 AI 面板功能较单一，需要更明确的 Agent 工作流与会话管理。
- **实现**：
  - 新增 `src/features/ai-assistant/` 模块，含 7 个 Agent、上下文选择器、会话列表。
  - Sidebar 新增 `AI 创作` / `AI Studio` 导航入口，路由注册 `/workspaces/:id/ai-assistant`。
  - 复用现有 AI IPC 与 `mock.ts` 模拟后端。
- **状态**：✅ 已完成，新增 `ai-assistant.spec.ts` E2E 测试

### 4. Rust clippy 清理
- **实现**：修复 `src-tauri/src/models/ai.rs` 与 `src-tauri/src/services/ai.rs` 中 6 处已有 clippy 告警，确保 `-D warnings` 通过。
- **状态**：✅ 已完成（代码修改通过旧测试二进制验证，本地因环境 bug 无法重新编译验证）

## 上一版本迭代回顾（v2.8.1）

### 1. 时间轴事件堆叠回归修复
- **问题**：v2.8.0 中创建多个事件时，同一轨道内的事件被错误分配到瀑布式子行，导致事件整体下移、占用额外垂直空间。
- **实现**：
  - 调整 `src/features/timeline/timelineLayout.ts` 的碰撞检测与瀑布式子行逻辑，确保非真正重叠的事件保持在一行内紧凑排布。
  - 在 `src/components/views/TimelineView.tsx` 中验证渲染位置与布局计算一致。
- **状态**：✅ 已完成，新增/更新时间轴子行分配回归测试

### 2. AI 助手输出稳定化
- **问题**：AI 助手流式输出时面板高度跳动、整段重渲染导致闪烁，长回复滚动不跟手。
- **实现**：
  - 为 AI 输出面板设置最小高度，减少布局偏移。
  - 采用增量纯文本渲染，仅更新新增内容。
  - 接入平滑滚动，长回复自动跟随最新输出。
- **状态**：✅ 已完成，新增 AI 输出稳定性单元/E2E 测试

### 3. 整体 UI 清爽化
- **问题**：v2.8.0 后工具栏、侧边栏、卡片、设置页与空状态的间距和阴影风格不够统一，视觉噪音较多。
- **实现**：
  - 统一各主要视图的间距 token 与卡片阴影层级。
  - 调整设置页与空状态的留白和文案，提升长时创作舒适度。
- **状态**：✅ 已完成，视觉回归通过 Playwright 截图对比

### 4. 测试提速
- **问题**：E2E 测试中存在较多 `waitForTimeout` 硬等待，本地运行耗时较长。
- **实现**：
  - 用显式条件等待替换不必要的 `waitForTimeout`。
  - 本地 Playwright 配置启用 2 workers，充分利用多核资源。
- **状态**：✅ 已完成，全量测试在本地稳定全绿（435 单元测试 + 39 E2E 用例）

## 上一版本迭代回顾（v2.8.0）

v2.8.0 基于 v2.7.5 的反馈，对前端动效、时间轴逻辑、设置页与新手引导进行系统性升级：

- **时间轴事件重叠修复与拖拽升级**：`src/features/timeline/timelineLayout.ts` 新增碰撞检测，时间重叠事件自动拆分到瀑布式子行；拖拽时显示 ghost 阴影与 snap hint，释放后播放归位动画。
- **动效系统 spring/layout token 与增强动效开关**：`src/lib/motion.ts` 扩展 token，`src/stores/ui.ts` 与 `src/components/views/SettingsView.tsx` 提供“增强动效”开关。
- **TimelineView 性能优化**：`React.memo`、`useMemo`、可视区裁剪，长滚动更流畅。
- **设置页重构**：分组、搜索、即时预览、中文说明。
- **首次进入工作区新手引导与各视图空状态优化**。
- **新增 `docs/前端优化指南.md`**。

- GitHub Release v2.8.0：<https://github.com/YJLZSL/Plotline/releases/tag/v2.8.0>

## 更早版本迭代回顾（v2.7.5）

v2.7.5 修复了 v2.7.4 CI 中发现的 RAG 关键词检索 `LIKE` 子查询缺少 `%` 通配符问题（`src-tauri/src/services/ai.rs`），导致检索结果始终返回 0 条；补全通配符后确保 AI RAG 检索能正确返回相关实体，CI/Release workflow 全绿。

- GitHub Release v2.7.5：<https://github.com/YJLZSL/Plotline/releases/tag/v2.7.5>

## v2.8.1 迭代状态

### 目标

修复 v2.8.0 中时间轴多个事件被错误堆叠到下方的回归问题，并快速打磨 AI 助手输出稳定性与整体 UI 清爽度，发布 v2.8.1 patch 版本。

### 已完成工作

- 时间轴事件堆叠回归修复：调整碰撞检测与瀑布式子行逻辑，保证非真正重叠的事件在一行内紧凑排布。
- AI 助手输出稳定化：最小高度、增量纯文本渲染、平滑滚动。
- 整体 UI 清爽化：统一工具栏/侧边栏/卡片/设置页/空状态的间距与阴影。
- 测试提速：减少 `waitForTimeout` 硬等待，本地 Playwright 启用 2 workers。
- 版本号统一升级到 `2.8.1`（`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.lock`）。
- `AGENTS.md`、`更新日志.md`、本文件、`docs/产品路线图.md` 更新至 v2.8.1 状态。
- 验证套件：`pnpm lint`、`pnpm typecheck`、`pnpm test:run`（435 单元测试）、`pnpm test:e2e`（39 E2E 用例）全绿；`cargo test` 与 `cargo clippy` 预期在 CI 中全绿。

### 发布与签名状态

- Release 签名自 v2.5.3 起已完全自动化；v2.8.1 继续由 CI 自动生成 `.exe`、`.msi`、`.sig` 与 `latest.json` 并上传 Release。
- 无需使用本地 `keys/plotline.key` 手动签名；本地密钥仅作为 CI Secret 失效时的紧急 fallback。
- 推送 tag `v2.8.1` 后，Release Workflow 的 `Sign installer and upload updater manifest` 步骤预期自动生成 `.sig` 与 `latest.json`。

### 下一版本建议（v2.9+）

- 地图：地点分组/图层、打印/PDF 导出、角色足迹连线。
- VN：角色立绘插槽拖拽、完整预览播放器、导出 Ren'Py 增强。
- 世界观：种族/物种/宗教等实体管理、设定冲突检测。
- AI：Agent 式工作流，自动整理大纲/发现时间轴漏洞；AI 输出版本回溯。
- UI 美术：统一空状态插画、卡片质感升级、日文翻译补全。
- 番茄钟：与写作目标绑定、每日统计、系统通知提醒。

## 🔐 签名密钥状态（更新于 2026-06-28）

**密钥位置**：本地仓库 `keys/plotline.key`（私钥）、`keys/plotline.key.pub`（公钥）
- 公钥内容：`dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDE1NDIwNDk4N0NBRTAxQzcKUldUSEFhNThtQVJDRlF0dk1EOGRRdzhUQkxKOWdCREpqdUpYMEVmMXF2bG1acTFSU3lodjVBNGoK`
- 与 `src-tauri/tauri.conf.json` 中的 `pubkey` 字段 **完全匹配** ✅
- `keys/` 在 `.gitignore` 中，**不会提交到 GitHub**

**GitHub Secret 状态**：`TAURI_SIGNING_PRIVATE_KEY` 已配置（2026-06-27 更新）

**自动更新签名状态**：
- v2.5.3 起，`.sig` 与 `latest.json` 已由 CI 自动生成并上传 Release，不再需要手动签名。
- CI 签名步骤依赖 GitHub Secret `TAURI_SIGNING_PRIVATE_KEY`；该 Secret 已配置且验证通过。
- **紧急 fallback**：若 CI 失败或 Secret 失效，可临时使用本地 `keys/plotline.key` 手动签名：
  1. 下载 CI 构建的 `.exe` 文件
  2. 使用 `tauri signer sign --private-key-path keys/plotline.key -- Plotline_x.y.z_x64-setup.exe`
  3. 将生成的 `.sig` 文件上传到 Release assets
  4. 创建 `releases/vx.y.z/latest.json` 并填入正确的签名和 URL

## 遗留问题与下一步任务

### 高优先级

1. **v2.7.4 已发布**：main 分支当前与 v2.7.4 Release 对齐，后续功能直接基于 main 开发。
2. **CI 自动签名待验证**：v2.7.4 的 `.sig` 与 `latest.json` 预期由 CI 自动生成并上传，无需手动 fallback；自动更新端点待推送 tag 后验证。

### 中优先级

1. **日文翻译补全**
2. **地图地点分组/图层**
3. **番茄钟与写作目标深度绑定**（每日字数统计、番茄钟联动）

## 构建命令

```bash
# 前端检查
./node_modules/.bin/eslint . --max-warnings=0
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run

# Rust 检查（需 cargo 可用）
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
```

---

## 附录：项目整理记录（2026-06-27）

### 本次整理内容

1. **安全与隐私检查**：
   - ✅ `.gitignore` 完整：`keys/`、`.env`、`*.db`、`releases/**/*.exe`、`.msi`、`.sig` 已排除
   - ✅ 远程 GitHub 仓库无 `keys/` 目录、无 `.env` 文件、无数据库文件

2. **文档整理**：
   - 更新 `交接文档.md` 至 v2.7.4 状态
   - 更新 `更新日志.md` 添加 v2.7.4 条目
   - 更新 `AGENTS.md` 与 `docs/产品路线图.md` 至 v2.7.4 状态
   - 所有文档状态更新为 v2.7.4 有效

3. **生产/环境/测试隔离**：
   - ✅ 开发配置仅在 `tauri.conf.json` 中，无生产环境密钥泄露
   - ✅ 构建产物在 `.gitignore` 中，不提交
   - ✅ 无 `.env` 文件、无数据库文件、无日志文件在仓库中

### 密钥状态

| 项目 | 状态 |
|---|---|
| 私钥（`keys/plotline.key`） | ✅ 本地存在，已加入 `.gitignore`，未提交 |
| 公钥（`keys/plotline.key.pub`） | ✅ 本地存在，已加入 `.gitignore`，未提交 |
| 公钥（`tauri.conf.json`） | ✅ 已嵌入客户端配置 |
| GitHub Secret（`TAURI_SIGNING_PRIVATE_KEY`） | ✅ 已配置（2026-06-26 更新） |

### 快速上手指南

新 AI 或开发者接手时，按以下顺序操作：

1. **读文档**（5 分钟）：
   - `AGENTS.md` → `交接文档.md` → `docs/密钥管理指南.md` → `产品需求与设计文档.md`

2. **检查环境**（2 分钟）：
   - 确认 `keys/plotline.key` 存在（如缺失，联系项目 owner）
   - 确认 `node_modules/` 已安装（如缺失，运行 `pnpm install`）

3. **运行项目**（2 分钟）：
   - 前端：`./node_modules/.bin/vite` 或 `pnpm dev`
   - 完整（含 Rust）：`pnpm tauri dev`（需 Rust 环境）

4. **修改前检查**（3 分钟）：
   - 读 `AGENTS.md` §1 仓库导航表，定位要修改的文件
   - 读至少 1 个相邻同类文件，理解现有约定
   - 执行检查：`./node_modules/.bin/tsc --noEmit` + `./node_modules/.bin/vitest run`

---

> **文档版本**：v0.5.7
> **最后更新**：2026-06-27（v2.7.4 已发布：修复 v2.7.3 CI SQLite ESCAPE 测试错误；CI/Release 预期成功，.sig 与 latest.json 由 CI 自动生成）
