# 交接文档 — Plotline v2.4.0

> 本文档记录当前版本状态、已完成工作、遗留问题与下一步任务，供下一任 AI 快速接手。

## 当前版本

- **版本号**：v2.4.0（已发布，CI 构建中）
- **上一版本**：v2.3.0（已发布）
- **Git tag**：`v2.4.0` 已推送
- **GitHub Release**：<https://github.com/YJLZSL/Plotline/releases/tag/v2.4.0>

## 已完成工作（v2.4.0 本轮迭代）

### 1. 大纲横向卡片模式
- **需求**："主界面横向可滑动，展示图片和章节名称"
- **实现**：大纲视图新增第三种「卡片视图」，按卷分组横向滚动，卡片展示封面图片与节点标题
- **数据库**：迁移 `010_outline_cover_image.sql` 给 `outline_nodes` 添加 `cover_image` 字段
- **状态**：✅ 已完成，类型检查全绿，测试通过

### 2. 时间轴事件地点字段
- **需求**："点击时间轴展开详情页，显示具体时间、地点、人物以及详情描述"
- **实现**：事件编辑对话框新增「地点」选择器，关联地图中的地点；后端 `location_id` 已完整支持
- **状态**：✅ 已完成

### 3. MC 主题素材丰富
- **需求**："MC 主题只有风格，没有 MC 素材"
- **实现**：新增工作台/熔炉/砖块/钻石矿石/TNT 5种像素纹理；自动应用到 Toolbar（圆石）、按钮（木板）、弹窗（石头）
- **状态**：✅ 已完成

## 🔐 签名密钥状态（更新于 2026-06-26）

**密钥位置**：本地仓库 `keys/plotline.key`（私钥）、`keys/plotline.key.pub`（公钥）
- 公钥内容：`dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDE1NDIwNDk4N0NBRTAxQzcKUldUSEFhNThtQVJDRlF0dk1EOGRRdzhUQkxKOWdCREpqdUpYMEVmMXF2bG1acTFSU3lodjVBNGoK`
- 与 `src-tauri/tauri.conf.json` 中的 `pubkey` 字段 **完全匹配** ✅
- `keys/` 在 `.gitignore` 中，**不会提交到 GitHub**

**当前问题**：GitHub Actions 的 `TAURI_SIGNING_PRIVATE_KEY` Secret 可能未配置，导致 CI 构建产物缺少 `.sig` 签名，自动更新失效。

**解决方案**：

**方案 A：配置 GitHub Secret（推荐）**
1. 将 `keys/plotline.key` 的完整内容复制到 GitHub 仓库 Settings → Secrets → Actions → `TAURI_SIGNING_PRIVATE_KEY`
2. 重新推送 tag 触发 CI（或删除后重新创建 `v2.4.0` tag）

**方案 B：手动签名（无需重新构建）**
1. 从 Release 页面下载 `Plotline_2.4.0_x64-setup.exe`
2. 使用本地密钥签名：`tauri signer sign --private-key-path keys/plotline.key -- Plotline_2.4.0_x64-setup.exe`
3. 生成 `.sig` 文件和 `latest.json`，上传到 Release

**方案 C：移除签名验证（不推荐）**
- 修改 `tauri.conf.json` 移除 `pubkey` 字段，但会降低安全性

**当前状态**：`releases/v2.4.0/latest.json` 已创建，signature 为 `<PLACEHOLDER>`，需替换为真实签名后生效。

## 遗留问题与下一步任务

### 高优先级

1. **自动更新签名补全**（见上方 🔐 签名密钥状态）
2. **CI 构建验证**：确认 Release 页面已上传 `.exe`、`.msi`、`.sig`、`latest.json` 四个文件

### 中优先级

3. **番茄钟与写作目标深度绑定**
4. **日文翻译补全**
5. **地图地点分组/图层**

## 构建命令

```bash
# 前端检查
./node_modules/.bin/eslint . --max-warnings=0
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/vitest run

# Rust 检查（需 cargo 可用）
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
```

---

> 文档版本：v0.3.0
> 最后更新：2026-06-26
